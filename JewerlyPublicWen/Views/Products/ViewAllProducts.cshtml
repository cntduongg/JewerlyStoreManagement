@using JewerlyPublicWen.Models.Dtos
@{
    ViewBag.Title = "Tất Cả Sản Phẩm";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<div style="background: yellow;">
    Type: @ViewBag.Categories?.GetType().Name
    Count: @ViewBag.Categories?.Count
</div>
<!-- Enhanced Luxury Header with Parallax -->
<section class="luxury-products-header-enhanced">
    <div class="parallax-background"></div>
    <div class="floating-particles"></div>
    <div class="header-overlay"></div>
    <div class="container">
        <div class="luxury-header-content" data-aos="zoom-in">
            <div class="luxury-badge-animated">
                <span class="badge-shine"></span>
                COLLECTION COMPLETE
            </div>
            <h1 class="enhanced-title">
                <span class="title-line">BỘ SƯU TẬP</span>
                <span class="title-main">TOÀN BỘ</span>
            </h1>
            <p class="enhanced-subtitle">
                <i class="fas fa-gem"></i>
                Khám phá tất cả thiết kế độc quyền của chúng tôi
                <i class="fas fa-gem"></i>
            </p>
            <div class="scroll-hint">
                <div class="scroll-mouse"></div>
                <span>Cuộn để khám phá</span>
            </div>
        </div>
    </div>
</section>

<!-- Enhanced Luxury Filter & Sort Section with Glassmorphism -->
<section class="luxury-controls-section-enhanced">
    <div class="container">
        <div class="glass-control-panel" data-aos="fade-up">
            <div class="control-panel-header">
                <h3><i class="fas fa-sliders-h"></i> BỘ LỌC & SẮP XẾP</h3>
                <button class="filter-toggle-btn" onclick="clearFilters()">
                    <i class="fas fa-redo"></i> Reset
                </button>
            </div>

            <div class="luxury-controls-grid-enhanced">
                <!-- Category Filter with Animation -->
                <div class="control-group-enhanced">
                    <label class="control-label">
                        <i class="fas fa-layer-group"></i>
                        DANH MỤC
                    </label>
                    <div class="filter-item-enhanced">
                        <select id="categoryFilter" class="luxury-select-enhanced">
                            <option value="">Tất Cả Danh Mục</option>
                            @if (ViewBag.Categories != null) 
                            {
                            @foreach (var cat in ViewBag.Categories)
                                                        {
                            <option value="@cat.CategoryId">@cat.CategoryName</option>
                                                        }
                            }
                        </select>
                        <div class="select-arrow">
                            <i class="fas fa-chevron-down"></i>
                        </div>
                    </div>
                </div>

                <!-- Sort Filter with Animation -->
                <div class="control-group-enhanced">
                    <label class="control-label">
                        <i class="fas fa-sort-amount-down"></i>
                        SẮP XẾP
                    </label>
                    <div class="filter-item-enhanced">
                        <select id="sortFilter" class="luxury-select-enhanced" onchange="sortProducts()">
                            <option value="newest">🆕 Mới Nhất</option>
                            <option value="price_asc">💰 Giá: Thấp → Cao</option>
                            <option value="price_desc">💎 Giá: Cao → Thấp</option>
                            <option value="name_asc">🔤 Tên: A-Z</option>
                            <option value="name_desc">🔠 Tên: Z-A</option>
                        </select>
                        <div class="select-arrow">
                            <i class="fas fa-chevron-down"></i>
                        </div>
                    </div>
                </div>

                <!-- Enhanced Search -->
                <div class="control-group-enhanced search-group-full">
                    <label class="control-label">
                        <i class="fas fa-search"></i>
                        TÌM KIẾM
                    </label>
                    <div class="luxury-search-container-enhanced">
                        <input type="text" id="searchInput" class="luxury-search-enhanced"
                               placeholder="Nhập tên sản phẩm, danh mục..." onkeyup="handleSearch()">
                        <button class="search-btn-enhanced" onclick="handleSearch()">
                            <i class="fas fa-search"></i>
                            <span>Tìm</span>
                        </button>
                    </div>
                </div>
            </div>

            <!-- Enhanced Results Info -->
            <div class="results-info-enhanced" id="resultsInfo">
                <div class="results-badge">
                    <i class="fas fa-check-circle"></i>
                    <span id="productCount">0</span> sản phẩm được tìm thấy
                </div>
            </div>
        </div>
    </div>
</section>

<!-- Enhanced Luxury Products Grid Section -->
<section class="luxury-products-grid-section-enhanced">
    <div class="container">
        <!-- Loading State with Diamond Animation -->
        <div id="loadingSpinner" class="luxury-loading-enhanced">
            <div class="luxury-spinner-enhanced">
                <div class="diamond-spinner-3d">
                    <div class="diamond-face"></div>
                    <div class="diamond-face"></div>
                    <div class="diamond-face"></div>
                    <div class="diamond-face"></div>
                </div>
                <div class="loading-text">
                    <h3>Đang tải bộ sưu tập</h3>
                    <div class="loading-dots">
                        <span></span><span></span><span></span>
                    </div>
                </div>
            </div>
        </div>

        <!-- Enhanced Products Grid -->
        <div id="productsGrid" class="luxury-products-grid-enhanced">
            <!-- Products will be loaded here -->
        </div>

        <!-- Enhanced Empty State -->
        <div id="emptyState" class="luxury-empty-state-enhanced" style="display: none;">
            <div class="empty-content-enhanced">
                <div class="empty-icon-container">
                    <i class="fas fa-gem"></i>
                    <div class="icon-glow"></div>
                </div>
                <h3>Không tìm thấy sản phẩm</h3>
                <p>Hãy thử tìm kiếm với từ khóa khác hoặc thay đổi bộ lọc</p>
                <button class="btn-luxury-primary-enhanced" onclick="clearFilters()">
                    <i class="fas fa-redo"></i>
                    <span>Xóa Bộ Lọc</span>
                </button>
            </div>
        </div>

        <!-- Enhanced Error State -->
        <div id="errorState" class="luxury-error-state-enhanced" style="display: none;">
            <div class="error-content-enhanced">
                <div class="error-icon-container">
                    <i class="fas fa-exclamation-triangle"></i>
                </div>
                <h3 id="errorMessage">Đã xảy ra lỗi</h3>
                <p>Vui lòng thử lại sau hoặc liên hệ với chúng tôi</p>
                <button class="btn-luxury-primary-enhanced" onclick="loadProducts()">
                    <i class="fas fa-sync-alt"></i>
                    <span>Thử Lại</span>
                </button>
            </div>
        </div>

        <!-- Enhanced Pagination -->
        <div id="pagination" class="luxury-pagination-enhanced" style="display: none;"></div>
    </div>
</section>

@section Scripts {
    <script>
        // ==================================================
        // CONFIGURATION
        // ==================================================
        const API_URL = 'https://localhost:7114/api/Products/GetAllProducts';
        let allProducts = [];
        let currentPage = 1;
        const productsPerPage = 12;
        let currentFilters = {
            category: '',
            search: '',
            sort: 'newest'
        };

        // ==================================================
        // MAIN FUNCTIONS
        // ==================================================
        async function loadProducts() {
            showLuxuryLoading();
            hideLuxuryError();
            hideEmptyState();

            try {
                console.log('Loading all products...');
                const response = await fetch(API_URL);

                if (response.ok) {
                    const products = await response.json();
                    allProducts = products;
                    applyFiltersAndDisplay();
                } else {
                    throw new Error(`HTTP error! status: ${response.status}`);
                }
            } catch (error) {
                console.error('Products API Error:', error);
                showLuxuryError('Không thể tải sản phẩm: ' + error.message);
                // Fallback to mock data
                allProducts = getMockProducts();
                applyFiltersAndDisplay();
            } finally {
                hideLuxuryLoading();
            }
        }

        function applyFiltersAndDisplay() {
            let filteredProducts = [...allProducts];

            // Apply category filter
            if (currentFilters.category) {
                filteredProducts = filteredProducts.filter(product =>
                    product.categoryId == currentFilters.category
                );
            }

            // Apply search filter
            if (currentFilters.search) {
                const searchTerm = currentFilters.search.toLowerCase();
                filteredProducts = filteredProducts.filter(product =>
                    product.productName.toLowerCase().includes(searchTerm) ||
                    (product.categoryName && product.categoryName.toLowerCase().includes(searchTerm)) ||
                    (product.shortDescription && product.shortDescription.toLowerCase().includes(searchTerm))
                );
            }

            // Apply sorting
            filteredProducts = sortProductsList(filteredProducts, currentFilters.sort);

            // Update results count with animation
            updateResultsCount(filteredProducts.length);

            // Display products
            displayProductsGrid(filteredProducts);
            setupPagination(filteredProducts);
        }

        function displayProductsGrid(products) {
            const grid = document.getElementById('productsGrid');
            const emptyState = document.getElementById('emptyState');

            grid.innerHTML = '';

            if (!products || products.length === 0) {
                grid.style.display = 'none';
                emptyState.style.display = 'flex';
                document.getElementById('pagination').style.display = 'none';
                return;
            }

            grid.style.display = 'grid';
            emptyState.style.display = 'none';

            // Calculate pagination
            const startIndex = (currentPage - 1) * productsPerPage;
            const endIndex = startIndex + productsPerPage;
            const paginatedProducts = products.slice(startIndex, endIndex);

            paginatedProducts.forEach((product, index) => {
                const imageUrl = getLuxuryImageUrl(product);
                const animationDelay = (index % 12) * 50;

                const productHtml = `
                    <div class="luxury-product-card-enhanced" data-aos="fade-up" data-aos-delay="${animationDelay}">
                        <div class="product-card-inner-enhanced">
                            <div class="product-badge-corner ${product.hasGem ? 'has-gem' : 'no-gem'}">
                                <i class="fas ${product.hasGem ? 'fa-gem' : 'fa-circle'}"></i>
                                <span>${product.hasGem ? 'ĐÁ QUÝ' : 'KHÔNG ĐÁ'}</span>
                            </div>

                            <div class="product-image-container-enhanced">
                                <div class="image-glow"></div>
                                <img src="${imageUrl}"
                                     alt="${product.productName}"
                                     class="luxury-product-image-enhanced"
                                     loading="lazy">
                                <div class="product-overlay-enhanced">
                                    <button class="quick-view-btn-enhanced" onclick="viewProductDetail(${product.productId})">
                                        <i class="fas fa-eye"></i>
                                        <span>Xem Chi Tiết</span>
                                    </button>
                                </div>
                            </div>

                            <div class="product-info-enhanced">
                                <div class="product-category-badge">
                                    ${getCategoryIcon(product.categoryId)}
                                    ${product.categoryName || 'Trang Sức'}
                                </div>

                                <h3 class="product-title-enhanced">${product.productName}</h3>

                                <p class="product-description-enhanced">
                                    ${product.shortDescription || 'Thiết kế độc đáo, tinh xảo'}
                                </p>

                                <div class="product-specs-enhanced">
                                    <div class="spec-item-enhanced">
                                        <i class="fas fa-weight"></i>
                                        <span>${product.goldWeight ? product.goldWeight + 'g' : 'N/A'}</span>
                                    </div>
                                    <div class="spec-divider"></div>
                                    <div class="spec-item-enhanced">
                                        <i class="fas fa-gem"></i>
                                        <span>${product.gemWeight ? product.gemWeight + 'ct' : 'N/A'}</span>
                                    </div>
                                </div>

                                <div class="product-price-section-enhanced">
                                    <div class="price-label">Giá</div>
                                    <div class="price-value">${formatPrice(product.sellingPrice)}</div>
                                </div>

                                <button class="btn-view-detail-enhanced" onclick="viewProductDetail(${product.productId})">
                                    <span>Khám Phá Ngay</span>
                                    <i class="fas fa-arrow-right"></i>
                                </button>
                            </div>
                        </div>
                    </div>
                `;

                grid.innerHTML += productHtml;
            });

            // Re-init AOS for new elements
            if (typeof AOS !== 'undefined') {
                AOS.refresh();
            }
        }

        function setupPagination(products) {
            const totalPages = Math.ceil(products.length / productsPerPage);
            const pagination = document.getElementById('pagination');

            if (totalPages <= 1) {
                pagination.style.display = 'none';
                return;
            }

            pagination.style.display = 'flex';
            pagination.innerHTML = '';

            // Previous button
            const prevBtn = document.createElement('button');
            prevBtn.className = 'pagination-btn-enhanced';
            prevBtn.innerHTML = '<i class="fas fa-chevron-left"></i>';
            prevBtn.disabled = currentPage === 1;
            prevBtn.onclick = () => {
                if (currentPage > 1) {
                    currentPage--;
                    applyFiltersAndDisplay();
                    scrollToTop();
                }
            };
            pagination.appendChild(prevBtn);

            // Page numbers with smart ellipsis
            const maxVisiblePages = 5;
            let startPage = Math.max(1, currentPage - Math.floor(maxVisiblePages / 2));
            let endPage = Math.min(totalPages, startPage + maxVisiblePages - 1);

            if (endPage - startPage < maxVisiblePages - 1) {
                startPage = Math.max(1, endPage - maxVisiblePages + 1);
            }

            // First page
            if (startPage > 1) {
                const firstBtn = createPageButton(1);
                pagination.appendChild(firstBtn);
                if (startPage > 2) {
                    const ellipsis = document.createElement('span');
                    ellipsis.className = 'pagination-ellipsis';
                    ellipsis.textContent = '...';
                    pagination.appendChild(ellipsis);
                }
            }

            // Page buttons
            for (let i = startPage; i <= endPage; i++) {
                const pageBtn = createPageButton(i);
                pagination.appendChild(pageBtn);
            }

            // Last page
            if (endPage < totalPages) {
                if (endPage < totalPages - 1) {
                    const ellipsis = document.createElement('span');
                    ellipsis.className = 'pagination-ellipsis';
                    ellipsis.textContent = '...';
                    pagination.appendChild(ellipsis);
                }
                const lastBtn = createPageButton(totalPages);
                pagination.appendChild(lastBtn);
            }

            // Next button
            const nextBtn = document.createElement('button');
            nextBtn.className = 'pagination-btn-enhanced';
            nextBtn.innerHTML = '<i class="fas fa-chevron-right"></i>';
            nextBtn.disabled = currentPage === totalPages;
            nextBtn.onclick = () => {
                if (currentPage < totalPages) {
                    currentPage++;
                    applyFiltersAndDisplay();
                    scrollToTop();
                }
            };
            pagination.appendChild(nextBtn);
        }

        function createPageButton(pageNum) {
            const btn = document.createElement('button');
            btn.className = `pagination-btn-enhanced ${pageNum === currentPage ? 'active' : ''}`;
            btn.textContent = pageNum;
            btn.onclick = () => {
                currentPage = pageNum;
                applyFiltersAndDisplay();
                scrollToTop();
            };
            return btn;
        }

        function scrollToTop() {
            window.scrollTo({ top: 0, behavior: 'smooth' });
        }

        // ==================================================
        // FILTER & SORT FUNCTIONS
        // ==================================================
        function filterProducts() {
                const categoryId = document.getElementById('categoryFilter').value;
        currentFilters.category = categoryId;
        currentPage = 1;
        applyFiltersAndDisplay();
        }

        function sortProducts() {
            currentFilters.sort = document.getElementById('sortFilter').value;
            currentPage = 1;
            applyFiltersAndDisplay();
        }

        function handleSearch() {
            currentFilters.search = document.getElementById('searchInput').value.trim();
            currentPage = 1;
            applyFiltersAndDisplay();
        }

        function clearFilters() {
            currentFilters = { category: '', search: '', sort: 'newest' };
            document.getElementById('categoryFilter').value = '';
            document.getElementById('sortFilter').value = 'newest';
            document.getElementById('searchInput').value = '';
            currentPage = 1;
            applyFiltersAndDisplay();
        }

        function sortProductsList(products, sortType) {
            const sorted = [...products];

            switch (sortType) {
                case 'newest':
                    return sorted.sort((a, b) => b.productId - a.productId);
                case 'price_asc':
                    return sorted.sort((a, b) => (a.productPrice || 0) - (b.productPrice || 0));
                case 'price_desc':
                    return sorted.sort((a, b) => (b.productPrice || 0) - (a.productPrice || 0));
                case 'name_asc':
                    return sorted.sort((a, b) => a.productName.localeCompare(b.productName));
                case 'name_desc':
                    return sorted.sort((a, b) => b.productName.localeCompare(a.productName));
                default:
                    return sorted;
            }
        }

        // ==================================================
        // UTILITY FUNCTIONS
        // ==================================================
               function getLuxuryImageUrl(product) {
            const rawImageUrl = product.mainImageUrl || product.MainImageUrl;

            if (!rawImageUrl || rawImageUrl.includes('img00') || !rawImageUrl.startsWith('http')) {
                const luxuryImages = {
                    1: 'https://images.unsplash.com/photo-1605100804763-247f67b3557e?w=400&h=400&fit=crop',
                    2: 'https://images.unsplash.com/photo-1599643478518-a784e5dc4c8f?w=400&h=400&fit=crop',
                    3: 'https://images.unsplash.com/photo-1588444650700-6c7f0c89d36b?w=400&h=400&fit=crop',
                    4: 'https://images.unsplash.com/photo-1535632066927-ab7c9ab60908?w=400&h=400&fit=crop',
                    5: 'https://images.unsplash.com/photo-1515562141207-7a88fb7ce338?w=400&h=400&fit=crop'
                };
                return luxuryImages[product.categoryId] || luxuryImages[2];
            }
            return rawImageUrl;
        }

        function getCategoryIcon(categoryId) {
            const icons = {
                1: '💍',
                2: '📿',
                3: '⌚',
                4: '💎',
                5: '🔶'
            };
            return icons[categoryId] || '✨';
        }

                function formatPrice(price) {
            // Luôn hiển thị giá, nếu không có thì hiển thị 0
            const priceValue = price || 0;
            return priceValue.toLocaleString('vi-VN') + ' ₫';
        }
        function viewProductDetail(productId) {
            window.location.href = `/Products/Details/${productId}`;
        }

        function updateResultsCount(count) {
            const countElement = document.getElementById('productCount');
            const resultsInfo = document.getElementById('resultsInfo');

            if (countElement) {
                // Animate count
                countElement.style.transform = 'scale(1.2)';
                setTimeout(() => {
                    countElement.textContent = count;
                    countElement.style.transform = 'scale(1)';
                }, 100);
            }

            resultsInfo.style.display = count > 0 ? 'block' : 'none';
        }

        // ==================================================
        // LOADING & ERROR STATES
        // ==================================================
        function showLuxuryLoading() {
            document.getElementById('loadingSpinner').style.display = 'flex';
            document.getElementById('productsGrid').style.display = 'none';
        }

        function hideLuxuryLoading() {
            document.getElementById('loadingSpinner').style.display = 'none';
        }

        function showLuxuryError(message) {
            const errorState = document.getElementById('errorState');
            const errorMessage = document.getElementById('errorMessage');
            errorMessage.textContent = message;
            errorState.style.display = 'flex';
        }

        function hideLuxuryError() {
            document.getElementById('errorState').style.display = 'none';
        }

        function hideEmptyState() {
            document.getElementById('emptyState').style.display = 'none';
        }

        // ==================================================
        // MOCK DATA
        // ==================================================
        function getMockProducts() {
            return [
                {
                    productId: 1,
                    productName: 'Nhẫn Kim Cương Vĩnh Cửu',
                    categoryId: 1,
                    categoryName: 'Nhẫn Cao Cấp',
                    productPrice: 45000000,
                    goldWeight: 5.2,
                    gemWeight: 1.5,
                    hasGem: true,
                    shortDescription: 'Nhẫn kim cương thiết kế tinh xảo, tượng trưng cho tình yêu vĩnh cửu',
                    imageUrl: 'https://images.unsplash.com/photo-1605100804763-247f67b3557e?w=500&h=500&fit=crop'
                }
            ];
        }

        // ==================================================
        // PARALLAX EFFECT
        // ==================================================
        window.addEventListener('scroll', () => {
            const parallax = document.querySelector('.parallax-background');
            if (parallax) {
                const scrolled = window.pageYOffset;
                parallax.style.transform = `translateY(${scrolled * 0.5}px)`;
            }
        });

        // ==================================================
        // INITIALIZATION
        // ==================================================
        document.addEventListener('DOMContentLoaded', () => {
            loadProducts();

            // Initialize AOS
            if (typeof AOS !== 'undefined') {
                AOS.init({
                    duration: 800,
                    easing: 'ease-out-cubic',
                    once: true,
                    offset: 100
                });
            }
        });
    </script>

    <style>
        /* ==================================================
                   ENHANCED LUXURY STYLES
                   ================================================== */

        :root {
            --luxury-gold: #d4af37;
            --luxury-gold-light: #f4d03f;
            --luxury-gold-dark: #b8941f;
            --luxury-dark: #1a1a1a;
            --luxury-dark-light: #2d2d2d;
            --transition-smooth: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
            --shadow-elevated: 0 20px 60px rgba(0,0,0,0.15);
            --shadow-soft: 0 10px 40px rgba(0,0,0,0.08);
        }

        /* Enhanced Header Section */
        .luxury-products-header-enhanced {
            position: relative;
            min-height: 60vh;
            display: flex;
            align-items: center;
            justify-content: center;
            overflow: hidden;
            background: linear-gradient(135deg, #1a1a1a 0%, #2d2d2d 100%);
        }

        .parallax-background {
            position: absolute;
            top: -50%;
            left: 0;
            width: 100%;
            height: 200%;
            background: radial-gradient(circle at 20% 50%, rgba(212, 175, 55, 0.1) 0%, transparent 50%), radial-gradient(circle at 80% 80%, rgba(244, 208, 63, 0.08) 0%, transparent 50%);
            z-index: 1;
        }

        .floating-particles {
            position: absolute;
            width: 100%;
            height: 100%;
            overflow: hidden;
            z-index: 2;
        }

            .floating-particles::before,
            .floating-particles::after {
                content: '';
                position: absolute;
                width: 300px;
                height: 300px;
                border-radius: 50%;
                background: radial-gradient(circle, rgba(212, 175, 55, 0.15) 0%, transparent 70%);
                animation: float 20s infinite ease-in-out;
            }

            .floating-particles::before {
                top: 10%;
                left: 10%;
                animation-delay: 0s;
            }

            .floating-particles::after {
                bottom: 10%;
                right: 10%;
                animation-delay: -10s;
            }

        @@keyframes float {
            0%, 100%

        {
            transform: translate(0, 0) scale(1);
        }

        25% {
            transform: translate(50px, -50px) scale(1.1);
        }

        50% {
            transform: translate(-30px, -80px) scale(0.9);
        }

        75% {
            transform: translate(30px, -40px) scale(1.05);
        }

        }

        .header-overlay {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: linear-gradient(180deg, transparent 0%, rgba(0,0,0,0.3) 100%);
            z-index: 3;
        }

        .luxury-header-content {
            position: relative;
            z-index: 10;
            text-align: center;
            padding: 3rem 1rem;
        }

        .luxury-badge-animated {
            position: relative;
            display: inline-block;
            padding: 0.8rem 2rem;
            background: linear-gradient(135deg, rgba(212, 175, 55, 0.2), rgba(244, 208, 63, 0.2));
            border: 2px solid var(--luxury-gold);
            border-radius: 50px;
            color: var(--luxury-gold);
            font-size: 0.85rem;
            font-weight: 700;
            letter-spacing: 0.2em;
            margin-bottom: 2rem;
            overflow: hidden;
            backdrop-filter: blur(10px);
        }

        .badge-shine {
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255,255,255,0.3), transparent);
            animation: badgeShine 3s infinite;
        }

        @@keyframes badgeShine {
            0%, 100%

        {
            left: -100%;
        }

        50% {
            left: 100%;
        }

        }

        .enhanced-title {
            margin: 0 0 2rem;
        }

        .title-line {
            display: block;
            font-size: 1.5rem;
            font-weight: 400;
            color: var(--luxury-gold);
            letter-spacing: 0.3em;
            text-transform: uppercase;
            margin-bottom: 0.5rem;
            animation: fadeInDown 1s ease-out;
        }

        .title-main {
            display: block;
            font-size: 4rem;
            font-weight: 800;
            background: linear-gradient(135deg, #ffffff, var(--luxury-gold-light));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            text-transform: uppercase;
            letter-spacing: 0.1em;
            animation: fadeInUp 1s ease-out 0.2s both;
        }

        @@keyframes fadeInDown {
            from

        {
            opacity: 0;
            transform: translateY(-30px);
        }

        to {
            opacity: 1;
            transform: translateY(0);
        }

        }

        @@keyframes fadeInUp {
            from

        {
            opacity: 0;
            transform: translateY(30px);
        }

        to {
            opacity: 1;
            transform: translateY(0);
        }

        }

        .enhanced-subtitle {
            font-size: 1.2rem;
            color: rgba(255,255,255,0.8);
            max-width: 600px;
            margin: 0 auto 3rem;
            line-height: 1.8;
            animation: fadeIn 1s ease-out 0.4s both;
        }

            .enhanced-subtitle i {
                color: var(--luxury-gold);
                margin: 0 0.5rem;
                animation: pulse 2s infinite;
            }

        @@keyframes fadeIn {
            from

        {
            opacity: 0;
        }

        to {
            opacity: 1;
        }

        }

        @@keyframes pulse {
            0%, 100%

        {
            transform: scale(1);
            opacity: 1;
        }

        50% {
            transform: scale(1.2);
            opacity: 0.8;
        }

        }

        .scroll-hint {
            display: flex;
            flex-direction: column;
            align-items: center;
            gap: 1rem;
            color: rgba(255,255,255,0.6);
            font-size: 0.9rem;
            letter-spacing: 0.1em;
            text-transform: uppercase;
        }

        .scroll-mouse {
            width: 30px;
            height: 50px;
            border: 2px solid rgba(212, 175, 55, 0.5);
            border-radius: 20px;
            position: relative;
        }

            .scroll-mouse::before {
                content: '';
                position: absolute;
                top: 8px;
                left: 50%;
                transform: translateX(-50%);
                width: 4px;
                height: 8px;
                background: var(--luxury-gold);
                border-radius: 2px;
                animation: scrollDown 2s infinite;
            }

        @@keyframes scrollDown {
            0%, 100%

        {
            top: 8px;
            opacity: 1;
        }

        50% {
            top: 20px;
            opacity: 0.5;
        }

        }

        /* Enhanced Controls Section with Glassmorphism */
        .luxury-controls-section-enhanced {
            padding: 3rem 0;
            background: linear-gradient(180deg, #f8f9fa 0%, #ffffff 100%);
        }

        .glass-control-panel {
            background: rgba(255, 255, 255, 0.7);
            backdrop-filter: blur(20px);
            border: 1px solid rgba(255, 255, 255, 0.5);
            border-radius: 30px;
            padding: 2.5rem;
            box-shadow: var(--shadow-soft);
        }

        .control-panel-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 2rem;
            padding-bottom: 1.5rem;
            border-bottom: 2px solid rgba(212, 175, 55, 0.2);
        }

            .control-panel-header h3 {
                font-size: 1.3rem;
                font-weight: 700;
                color: var(--luxury-dark);
                display: flex;
                align-items: center;
                gap: 0.8rem;
                margin: 0;
            }

            .control-panel-header i {
                color: var(--luxury-gold);
            }

        .filter-toggle-btn {
            padding: 0.8rem 1.5rem;
            background: transparent;
            border: 2px solid var(--luxury-gold);
            border-radius: 15px;
            color: var(--luxury-gold);
            font-weight: 700;
            cursor: pointer;
            transition: var(--transition-smooth);
            display: flex;
            align-items: center;
            gap: 0.5rem;
        }

            .filter-toggle-btn:hover {
                background: var(--luxury-gold);
                color: white;
                transform: translateY(-2px);
                box-shadow: 0 5px 15px rgba(212, 175, 55, 0.3);
            }

        .luxury-controls-grid-enhanced {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
            gap: 2rem;
        }

        .control-group-enhanced {
            display: flex;
            flex-direction: column;
            gap: 0.8rem;
        }

        .search-group-full {
            grid-column: 1 / -1;
        }

        .control-label {
            font-size: 0.85rem;
            font-weight: 700;
            color: var(--luxury-dark);
            text-transform: uppercase;
            letter-spacing: 0.1em;
            display: flex;
            align-items: center;
            gap: 0.5rem;
        }

            .control-label i {
                color: var(--luxury-gold);
            }

        .filter-item-enhanced {
            position: relative;
        }

        .luxury-select-enhanced {
            width: 100%;
            padding: 1rem 3rem 1rem 1.5rem;
            background: white;
            border: 2px solid rgba(212, 175, 55, 0.3);
            border-radius: 15px;
            font-size: 1rem;
            font-weight: 600;
            color: var(--luxury-dark);
            cursor: pointer;
            transition: var(--transition-smooth);
            appearance: none;
        }

            .luxury-select-enhanced:hover {
                border-color: var(--luxury-gold);
                box-shadow: 0 5px 20px rgba(212, 175, 55, 0.15);
            }

            .luxury-select-enhanced:focus {
                outline: none;
                border-color: var(--luxury-gold);
                box-shadow: 0 0 0 4px rgba(212, 175, 55, 0.1);
            }

        .select-arrow {
            position: absolute;
            right: 1.2rem;
            top: 50%;
            transform: translateY(-50%);
            color: var(--luxury-gold);
            pointer-events: none;
            font-size: 0.9rem;
        }

        .luxury-search-container-enhanced {
            display: flex;
            gap: 0.5rem;
        }

        .luxury-search-enhanced {
            flex: 1;
            padding: 1rem 1.5rem;
            background: white;
            border: 2px solid rgba(212, 175, 55, 0.3);
            border-radius: 15px;
            font-size: 1rem;
            font-weight: 500;
            color: var(--luxury-dark);
            transition: var(--transition-smooth);
        }

            .luxury-search-enhanced:hover {
                border-color: var(--luxury-gold);
                box-shadow: 0 5px 20px rgba(212, 175, 55, 0.15);
            }

            .luxury-search-enhanced:focus {
                outline: none;
                border-color: var(--luxury-gold);
                box-shadow: 0 0 0 4px rgba(212, 175, 55, 0.1);
            }

            .luxury-search-enhanced::placeholder {
                color: #999;
            }

        .search-btn-enhanced {
            padding: 1rem 2rem;
            background: linear-gradient(135deg, var(--luxury-gold), var(--luxury-gold-light));
            border: none;
            border-radius: 15px;
            color: white;
            font-weight: 700;
            cursor: pointer;
            transition: var(--transition-smooth);
            display: flex;
            align-items: center;
            gap: 0.5rem;
            white-space: nowrap;
        }

            .search-btn-enhanced:hover {
                transform: translateY(-2px);
                box-shadow: 0 10px 30px rgba(212, 175, 55, 0.4);
            }

        .results-info-enhanced {
            margin-top: 2rem;
            text-align: center;
            display: none;
        }

        .results-badge {
            display: inline-flex;
            align-items: center;
            gap: 0.8rem;
            padding: 1rem 2rem;
            background: linear-gradient(135deg, rgba(34, 193, 195, 0.1), rgba(45, 253, 157, 0.1));
            border: 2px solid rgba(34, 193, 195, 0.3);
            border-radius: 50px;
            color: #00796b;
            font-weight: 700;
            font-size: 1rem;
        }

            .results-badge i {
                font-size: 1.2rem;
                color: #00c853;
            }

            .results-badge span {
                font-size: 1.3rem;
                color: var(--luxury-gold);
                transition: var(--transition-smooth);
            }

        /* Enhanced Products Grid */
        .luxury-products-grid-section-enhanced {
            padding: 4rem 0 6rem;
            background: #f8f9fa;
            min-height: 60vh;
        }

        .luxury-products-grid-enhanced {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(320px, 1fr));
            gap: 2.5rem;
            margin: 3rem 0;
        }

        .luxury-product-card-enhanced {
            perspective: 1500px;
        }

        .product-card-inner-enhanced {
            position: relative;
            width: 100%;
            min-height: 580px;
            background: white;
            border-radius: 25px;
            overflow: hidden;
            box-shadow: var(--shadow-soft);
            transition: var(--transition-smooth);
            transform-style: preserve-3d;
        }

        .luxury-product-card-enhanced:hover .product-card-inner-enhanced {
            transform: translateY(-15px) rotateX(2deg);
            box-shadow: var(--shadow-elevated);
        }

        .product-badge-corner {
            position: absolute;
            top: 1rem;
            right: 1rem;
            padding: 0.6rem 1.2rem;
            border-radius: 50px;
            display: flex;
            align-items: center;
            gap: 0.5rem;
            font-size: 0.75rem;
            font-weight: 700;
            letter-spacing: 0.1em;
            z-index: 10;
            backdrop-filter: blur(10px);
        }

            .product-badge-corner.has-gem {
                background: linear-gradient(135deg, rgba(212, 175, 55, 0.95), rgba(244, 208, 63, 0.95));
                color: white;
                box-shadow: 0 4px 15px rgba(212, 175, 55, 0.4);
            }

            .product-badge-corner.no-gem {
                background: rgba(0, 0, 0, 0.7);
                color: white;
                box-shadow: 0 4px 15px rgba(0, 0, 0, 0.3);
            }

        .product-image-container-enhanced {
            position: relative;
            height: 280px;
            overflow: hidden;
            background: linear-gradient(135deg, #f8f9fa 0%, #ffffff 100%);
        }

        .image-glow {
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            width: 150%;
            height: 150%;
            background: radial-gradient(circle, rgba(212, 175, 55, 0.2) 0%, transparent 70%);
            opacity: 0;
            transition: var(--transition-smooth);
        }

        .luxury-product-card-enhanced:hover .image-glow {
            opacity: 1;
            animation: glowPulse 2s infinite;
        }

        @@keyframes glowPulse {
            0%, 100%

        {
            transform: translate(-50%, -50%) scale(1);
        }

        50% {
            transform: translate(-50%, -50%) scale(1.1);
        }

        }

        .luxury-product-image-enhanced {
            width: 100%;
            height: 100%;
            object-fit: cover;
            transition: var(--transition-smooth);
        }

        .luxury-product-card-enhanced:hover .luxury-product-image-enhanced {
            transform: scale(1.1) rotate(2deg);
        }

        .product-overlay-enhanced {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: linear-gradient(135deg, rgba(26, 26, 26, 0.95), rgba(45, 45, 45, 0.9));
            display: flex;
            align-items: center;
            justify-content: center;
            opacity: 0;
            transition: var(--transition-smooth);
        }

        .luxury-product-card-enhanced:hover .product-overlay-enhanced {
            opacity: 1;
        }

        .quick-view-btn-enhanced {
            padding: 1rem 2rem;
            background: linear-gradient(135deg, var(--luxury-gold), var(--luxury-gold-light));
            border: none;
            border-radius: 15px;
            color: white;
            font-weight: 700;
            cursor: pointer;
            display: flex;
            align-items: center;
            gap: 0.8rem;
            font-size: 1rem;
            transition: var(--transition-smooth);
            transform: translateY(20px);
        }

        .luxury-product-card-enhanced:hover .quick-view-btn-enhanced {
            transform: translateY(0);
        }

        .quick-view-btn-enhanced:hover {
            transform: translateY(-5px);
            box-shadow: 0 10px 30px rgba(212, 175, 55, 0.5);
        }

        .product-info-enhanced {
            padding: 1.8rem;
            display: flex;
            flex-direction: column;
            gap: 1rem;
        }

        .product-category-badge {
            display: inline-flex;
            align-items: center;
            gap: 0.5rem;
            padding: 0.5rem 1rem;
            background: linear-gradient(135deg, rgba(212, 175, 55, 0.1), rgba(244, 208, 63, 0.1));
            border-radius: 50px;
            color: var(--luxury-gold-dark);
            font-size: 0.8rem;
            font-weight: 700;
            letter-spacing: 0.05em;
            width: fit-content;
            text-transform: uppercase;
        }

        .product-title-enhanced {
            font-size: 1.3rem;
            font-weight: 700;
            color: var(--luxury-dark);
            margin: 0;
            line-height: 1.4;
            display: -webkit-box;
            -webkit-line-clamp: 2;
            -webkit-box-orient: vertical;
            overflow: hidden;
        }

        .product-description-enhanced {
            color: #666;
            line-height: 1.6;
            font-size: 0.95rem;
            display: -webkit-box;
            -webkit-line-clamp: 2;
            -webkit-box-orient: vertical;
            overflow: hidden;
            margin: 0;
        }

        .product-specs-enhanced {
            display: flex;
            align-items: center;
            gap: 1rem;
            padding: 1rem;
            background: #f8f9fa;
            border-radius: 12px;
        }

        .spec-item-enhanced {
            display: flex;
            align-items: center;
            gap: 0.5rem;
            color: var(--luxury-dark);
            font-weight: 600;
            font-size: 0.9rem;
        }

            .spec-item-enhanced i {
                color: var(--luxury-gold);
            }

        .spec-divider {
            width: 2px;
            height: 20px;
            background: rgba(212, 175, 55, 0.3);
        }

        .product-price-section-enhanced {
            padding: 1.2rem;
            background: linear-gradient(135deg, rgba(212, 175, 55, 0.05), rgba(244, 208, 63, 0.05));
            border-radius: 15px;
            text-align: center;
            border: 2px dashed rgba(212, 175, 55, 0.2);
        }

        .price-label {
            font-size: 0.8rem;
            color: #666;
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 0.1em;
            margin-bottom: 0.3rem;
        }

        .price-value {
            font-size: 1.6rem;
            font-weight: 800;
            background: linear-gradient(135deg, var(--luxury-gold), var(--luxury-gold-light));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        .btn-view-detail-enhanced {
            width: 100%;
            padding: 1rem;
            background: linear-gradient(135deg, var(--luxury-dark), var(--luxury-dark-light));
            border: none;
            border-radius: 15px;
            color: white;
            font-weight: 700;
            cursor: pointer;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 0.8rem;
            font-size: 1rem;
            text-transform: uppercase;
            letter-spacing: 0.05em;
            transition: var(--transition-smooth);
        }

            .btn-view-detail-enhanced:hover {
                background: linear-gradient(135deg, var(--luxury-gold), var(--luxury-gold-light));
                transform: translateY(-3px);
                box-shadow: 0 10px 30px rgba(212, 175, 55, 0.4);
            }

            .btn-view-detail-enhanced i {
                transition: var(--transition-smooth);
            }

            .btn-view-detail-enhanced:hover i {
                transform: translateX(5px);
            }

        /* Enhanced Loading State */
        .luxury-loading-enhanced {
            display: none;
            justify-content: center;
            align-items: center;
            padding: 6rem 0;
            flex-direction: column;
        }

        .diamond-spinner-3d {
            width: 100px;
            height: 100px;
            position: relative;
            transform-style: preserve-3d;
            animation: spin3D 3s infinite linear;
        }

        @@keyframes spin3D {
            0%

        {
            transform: rotateX(0deg) rotateY(0deg);
        }

        100% {
            transform: rotateX(360deg) rotateY(360deg);
        }

        }

        .diamond-face {
            position: absolute;
            width: 100px;
            height: 100px;
            background: linear-gradient(135deg, var(--luxury-gold), var(--luxury-gold-light));
            border: 2px solid rgba(255, 255, 255, 0.5);
        }

            .diamond-face:nth-child(1) {
                transform: rotateY(0deg) translateZ(50px);
            }

            .diamond-face:nth-child(2) {
                transform: rotateY(90deg) translateZ(50px);
            }

            .diamond-face:nth-child(3) {
                transform: rotateY(180deg) translateZ(50px);
            }

            .diamond-face:nth-child(4) {
                transform: rotateY(-90deg) translateZ(50px);
            }

        .loading-text {
            margin-top: 3rem;
            text-align: center;
        }

            .loading-text h3 {
                font-size: 1.5rem;
                font-weight: 700;
                color: var(--luxury-dark);
                margin-bottom: 1rem;
            }

        .loading-dots {
            display: flex;
            gap: 0.5rem;
            justify-content: center;
        }

            .loading-dots span {
                width: 10px;
                height: 10px;
                background: var(--luxury-gold);
                border-radius: 50%;
                animation: dotPulse 1.5s infinite;
            }

                .loading-dots span:nth-child(2) {
                    animation-delay: 0.2s;
                }

                .loading-dots span:nth-child(3) {
                    animation-delay: 0.4s;
                }

        @@keyframes dotPulse {
            0%, 100%

        {
            transform: scale(1);
            opacity: 1;
        }

        50% {
            transform: scale(1.5);
            opacity: 0.5;
        }

        }

        /* Enhanced Empty State */
        .luxury-empty-state-enhanced {
            display: none;
            justify-content: center;
            align-items: center;
            padding: 6rem 0;
        }

        .empty-content-enhanced {
            text-align: center;
            max-width: 500px;
        }

        .empty-icon-container {
            position: relative;
            width: 120px;
            height: 120px;
            margin: 0 auto 2rem;
        }

            .empty-icon-container i {
                font-size: 5rem;
                color: var(--luxury-gold);
                opacity: 0.5;
                animation: float 3s infinite ease-in-out;
            }

        .icon-glow {
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            width: 150px;
            height: 150px;
            background: radial-gradient(circle, rgba(212, 175, 55, 0.3) 0%, transparent 70%);
            border-radius: 50%;
            animation: glowPulse 2s infinite;
        }

        .empty-content-enhanced h3 {
            font-size: 2rem;
            font-weight: 700;
            color: var(--luxury-dark);
            margin-bottom: 1rem;
        }

        .empty-content-enhanced p {
            font-size: 1.1rem;
            color: #666;
            margin-bottom: 2rem;
            line-height: 1.6;
        }

        /* Enhanced Error State */
        .luxury-error-state-enhanced {
            display: none;
            justify-content: center;
            align-items: center;
            padding: 6rem 0;
        }

        .error-content-enhanced {
            text-align: center;
            max-width: 500px;
        }

        .error-icon-container i {
            font-size: 5rem;
            color: #f44336;
            margin-bottom: 2rem;
            animation: shake 0.5s infinite;
        }

        @@keyframes shake {
            0%, 100%

        {
            transform: translateX(0);
        }

        25% {
            transform: translateX(-10px);
        }

        75% {
            transform: translateX(10px);
        }

        }

        .error-content-enhanced h3 {
            font-size: 2rem;
            font-weight: 700;
            color: var(--luxury-dark);
            margin-bottom: 1rem;
        }

        .error-content-enhanced p {
            font-size: 1.1rem;
            color: #666;
            margin-bottom: 2rem;
        }

        .btn-luxury-primary-enhanced {
            padding: 1rem 2.5rem;
            background: linear-gradient(135deg, var(--luxury-gold), var(--luxury-gold-light));
            border: none;
            border-radius: 15px;
            color: white;
            font-weight: 700;
            font-size: 1rem;
            cursor: pointer;
            display: inline-flex;
            align-items: center;
            gap: 0.8rem;
            text-transform: uppercase;
            letter-spacing: 0.05em;
            transition: var(--transition-smooth);
        }

            .btn-luxury-primary-enhanced:hover {
                transform: translateY(-3px);
                box-shadow: 0 10px 30px rgba(212, 175, 55, 0.4);
            }

        /* Enhanced Pagination */
        .luxury-pagination-enhanced {
            display: none;
            justify-content: center;
            align-items: center;
            gap: 0.8rem;
            margin-top: 4rem;
            flex-wrap: wrap;
        }

        .pagination-btn-enhanced {
            width: 50px;
            height: 50px;
            border: 2px solid var(--luxury-gold);
            background: white;
            color: var(--luxury-gold);
            border-radius: 12px;
            display: flex;
            align-items: center;
            justify-content: center;
            cursor: pointer;
            transition: var(--transition-smooth);
            font-weight: 700;
            font-size: 1rem;
        }

            .pagination-btn-enhanced:hover:not(:disabled) {
                background: var(--luxury-gold);
                color: white;
                transform: translateY(-3px);
                box-shadow: 0 8px 20px rgba(212, 175, 55, 0.3);
            }

            .pagination-btn-enhanced.active {
                background: linear-gradient(135deg, var(--luxury-gold), var(--luxury-gold-light));
                color: white;
                border-color: var(--luxury-gold-light);
                box-shadow: 0 5px 20px rgba(212, 175, 55, 0.4);
            }

            .pagination-btn-enhanced:disabled {
                opacity: 0.3;
                cursor: not-allowed;
            }

        .pagination-ellipsis {
            padding: 0 1rem;
            color: #999;
            font-weight: 700;
        }

        /* Responsive Design */
        @@media (max-width: 1200px) {
            .luxury-products-grid-enhanced

        {
            grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
            gap: 2rem;
        }

        }

        @@media (max-width: 768px) {
            .title-main

        {
            font-size: 2.5rem;
        }

        .glass-control-panel {
            padding: 1.5rem;
            border-radius: 20px;
        }

        .control-panel-header {
            flex-direction: column;
            gap: 1rem;
            align-items: flex-start;
        }

        .luxury-controls-grid-enhanced {
            grid-template-columns: 1fr;
            gap: 1.5rem;
        }

        .luxury-products-grid-enhanced {
            grid-template-columns: repeat(auto-fill, minmax(260px, 1fr));
            gap: 1.5rem;
        }

        .product-card-inner-enhanced {
            min-height: 550px;
        }

        .pagination-btn-enhanced {
            width: 45px;
            height: 45px;
        }

        }

        @@media (max-width: 480px) {
            .title-main

        {
            font-size: 2rem;
        }

        .luxury-products-grid-enhanced {
            grid-template-columns: 1fr;
        }

        .glass-control-panel {
            padding: 1.2rem;
        }

        .pagination-btn-enhanced {
            width: 40px;
            height: 40px;
            font-size: 0.9rem;
        }

        }
    </style>
}
