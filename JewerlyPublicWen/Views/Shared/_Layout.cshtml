<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewBag.Title - Jewelry Store</title>

    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Font Awesome -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <!-- Google Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;600;700&family=Montserrat:wght@300;400;500;600;700&display=swap" rel="stylesheet">

    <style>
        :root {
            --luxury-gold: #D4AF37;
            --luxury-gold-light: #E8C547;
            --luxury-dark: #0a0a0a;
            --luxury-dark-secondary: #1a1a1a;
            --transition-smooth: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Montserrat', sans-serif;
            overflow-x: hidden;
        }

        h1, h2, h3, h4, h5, h6 {
            font-family: 'Playfair Display', serif;
        }

        /* ==================== LUXURY NAVBAR ==================== */
        .luxury-navbar {
            background: linear-gradient(135deg, rgba(10, 10, 10, 0.98) 0%, rgba(26, 26, 26, 0.98) 100%);
            backdrop-filter: blur(10px);
            box-shadow: 0 5px 30px rgba(0, 0, 0, 0.3);
            padding: 1rem 0;
            position: sticky;
            top: 0;
            z-index: 1000;
            border-bottom: 1px solid rgba(212, 175, 55, 0.2);
        }

        .navbar-brand {
            font-family: 'Playfair Display', serif;
            font-weight: 700;
            font-size: 1.8rem;
            color: var(--luxury-gold) !important;
            display: flex;
            align-items: center;
            gap: 0.8rem;
            transition: var(--transition-smooth);
        }

            .navbar-brand i {
                font-size: 2rem;
                animation: gemRotate 4s infinite ease-in-out;
            }

        @@keyframes gemRotate {
            0%, 100%

        {
            transform: rotate(0deg) scale(1);
        }

        50% {
            transform: rotate(180deg) scale(1.1);
        }

        }

        .navbar-brand:hover {
            transform: scale(1.05);
            color: var(--luxury-gold-light) !important;
        }

        .nav-link {
            font-weight: 600;
            color: rgba(255, 255, 255, 0.8) !important;
            transition: var(--transition-smooth);
            padding: 0.7rem 1.2rem !important;
            border-radius: 8px;
            display: flex;
            align-items: center;
            gap: 0.5rem;
            position: relative;
            overflow: hidden;
        }

            .nav-link::before {
                content: '';
                position: absolute;
                bottom: 0;
                left: 50%;
                transform: translateX(-50%);
                width: 0;
                height: 2px;
                background: var(--luxury-gold);
                transition: width 0.3s ease;
            }

            .nav-link:hover::before,
            .nav-link.active::before {
                width: 80%;
            }

            .nav-link:hover {
                color: var(--luxury-gold) !important;
                background: rgba(212, 175, 55, 0.1);
            }

            .nav-link.active {
                color: var(--luxury-gold) !important;
                background: rgba(212, 175, 55, 0.15);
            }

        .navbar-toggler {
            border: 2px solid var(--luxury-gold);
            padding: 0.5rem 0.8rem;
            border-radius: 8px;
        }

            .navbar-toggler:focus {
                box-shadow: 0 0 0 0.2rem rgba(212, 175, 55, 0.3);
            }

        .navbar-toggler-icon {
            background-image: url("data:image/svg+xml,%3csvg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 30 30'%3e%3cpath stroke='%23D4AF37' stroke-linecap='round' stroke-miterlimit='10' stroke-width='2' d='M4 7h22M4 15h22M4 23h22'/%3e%3c/svg%3e");
        }

        .navbar-nav {
            gap: 0.5rem;
        }

        /* Phone Number Styling */
        .nav-link[href^="tel"] {
            background: linear-gradient(135deg, rgba(212, 175, 55, 0.2), rgba(232, 197, 71, 0.2));
            border: 1px solid var(--luxury-gold);
            font-weight: 700;
        }

            .nav-link[href^="tel"]:hover {
                background: linear-gradient(135deg, var(--luxury-gold), var(--luxury-gold-light));
                color: var(--luxury-dark) !important;
            }

        /* ==================== LUXURY FOOTER ==================== */
        .luxury-footer {
            background: linear-gradient(135deg, #0a0a0a 0%, #1a1a1a 100%);
            color: rgba(255, 255, 255, 0.8);
            padding: 4rem 0 2rem;
            position: relative;
            overflow: hidden;
        }

            .luxury-footer::before {
                content: '';
                position: absolute;
                top: 0;
                left: 0;
                width: 100%;
                height: 2px;
                background: linear-gradient(90deg, transparent, var(--luxury-gold), transparent);
            }

        .footer-brand {
            font-family: 'Playfair Display', serif;
            font-size: 1.8rem;
            font-weight: 700;
            color: var(--luxury-gold);
            display: flex;
            align-items: center;
            gap: 0.8rem;
            margin-bottom: 1rem;
        }

            .footer-brand i {
                font-size: 2rem;
            }

        .footer-description {
            color: rgba(255, 255, 255, 0.6);
            font-size: 0.95rem;
            line-height: 1.6;
            max-width: 300px;
        }

        .footer-section-title {
            font-family: 'Playfair Display', serif;
            color: var(--luxury-gold);
            font-size: 1.3rem;
            font-weight: 600;
            margin-bottom: 1.5rem;
            position: relative;
            padding-bottom: 0.8rem;
        }

            .footer-section-title::after {
                content: '';
                position: absolute;
                bottom: 0;
                left: 0;
                width: 50px;
                height: 2px;
                background: var(--luxury-gold);
            }

        .footer-links {
            list-style: none;
            padding: 0;
        }

            .footer-links li {
                margin-bottom: 0.8rem;
            }

        .footer-link {
            color: rgba(255, 255, 255, 0.7);
            text-decoration: none;
            transition: var(--transition-smooth);
            display: flex;
            align-items: center;
            gap: 0.5rem;
            font-weight: 500;
        }

            .footer-link i {
                color: var(--luxury-gold);
                font-size: 0.9rem;
            }

            .footer-link:hover {
                color: var(--luxury-gold);
                transform: translateX(5px);
            }

        .footer-contact-item {
            display: flex;
            align-items: center;
            gap: 1rem;
            margin-bottom: 1.2rem;
            color: rgba(255, 255, 255, 0.7);
        }

        .footer-contact-icon {
            width: 45px;
            height: 45px;
            border-radius: 50%;
            background: rgba(212, 175, 55, 0.1);
            border: 2px solid var(--luxury-gold);
            display: flex;
            align-items: center;
            justify-content: center;
            color: var(--luxury-gold);
            font-size: 1.1rem;
            transition: var(--transition-smooth);
        }

        .footer-contact-item:hover .footer-contact-icon {
            background: var(--luxury-gold);
            color: white;
            transform: scale(1.1);
        }

        .footer-contact-text {
            display: flex;
            flex-direction: column;
        }

        .footer-contact-label {
            font-size: 0.75rem;
            color: rgba(255, 255, 255, 0.5);
            text-transform: uppercase;
            letter-spacing: 0.1em;
        }

        .footer-contact-value {
            font-weight: 600;
            font-size: 1rem;
        }

        .footer-social {
            display: flex;
            gap: 1rem;
            margin-top: 1.5rem;
        }

        .social-icon {
            width: 45px;
            height: 45px;
            border-radius: 50%;
            background: rgba(212, 175, 55, 0.1);
            border: 2px solid var(--luxury-gold);
            display: flex;
            align-items: center;
            justify-content: center;
            color: var(--luxury-gold);
            text-decoration: none;
            transition: var(--transition-smooth);
            font-size: 1.1rem;
        }

            .social-icon:hover {
                background: var(--luxury-gold);
                color: white;
                transform: translateY(-5px);
                box-shadow: 0 10px 25px rgba(212, 175, 55, 0.4);
            }

        .footer-bottom {
            margin-top: 3rem;
            padding-top: 2rem;
            border-top: 1px solid rgba(212, 175, 55, 0.2);
            text-align: center;
        }

        .footer-copyright {
            color: rgba(255, 255, 255, 0.6);
            font-size: 0.9rem;
            margin: 0;
        }

        .footer-heart {
            color: #e74c3c;
            animation: heartbeat 1.5s infinite;
        }

        @@keyframes heartbeat {
            0%, 100%

        {
            transform: scale(1);
        }

        25% {
            transform: scale(1.2);
        }

        50% {
            transform: scale(1);
        }

        }

        /* ==================== SCROLL TO TOP BUTTON ==================== */
        .scroll-to-top {
            position: fixed;
            bottom: 2rem;
            right: 2rem;
            width: 55px;
            height: 55px;
            border-radius: 50%;
            background: linear-gradient(135deg, var(--luxury-gold), var(--luxury-gold-light));
            color: white;
            border: none;
            cursor: pointer;
            display: none;
            align-items: center;
            justify-content: center;
            font-size: 1.3rem;
            box-shadow: 0 10px 30px rgba(212, 175, 55, 0.4);
            transition: var(--transition-smooth);
            z-index: 999;
        }

            .scroll-to-top.show {
                display: flex;
            }

            .scroll-to-top:hover {
                transform: translateY(-5px) scale(1.1);
                box-shadow: 0 15px 40px rgba(212, 175, 55, 0.6);
            }

        /* ==================== RESPONSIVE ==================== */
        @@media (max-width: 992px) {
            .navbar-collapse

        {
            background: rgba(10, 10, 10, 0.98);
            padding: 1rem;
            border-radius: 10px;
            margin-top: 1rem;
            border: 1px solid rgba(212, 175, 55, 0.2);
        }

        .nav-link {
            margin-bottom: 0.5rem;
        }

        }

        @@media (max-width: 768px) {
            .luxury-footer

        {
            padding: 3rem 0 1.5rem;
        }

        .footer-section-title {
            font-size: 1.2rem;
            margin-top: 2rem;
        }

        .footer-brand {
            font-size: 1.5rem;
        }

        }
    </style>
</head>
<body>
    <!-- Luxury Navigation -->
    <nav class="navbar navbar-expand-lg luxury-navbar">
        <div class="container">
            <a class="navbar-brand" href="@Url.Action("Index", "Home")">
                <i class="fas fa-gem"></i>
                <span>Jewelry Store</span>
            </a>

            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>

            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav me-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="@Url.Action("Index", "Home")">
                            <i class="fas fa-home"></i>
                            <span>Trang Chủ</span>
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link active" href="@Url.Action("Products", "Public")">
                            <i class="fas fa-store"></i>
                            <span>Sản Phẩm</span>
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="@Url.Action("About", "Public")">
                            <i class="fas fa-info-circle"></i>
                            <span>Về Chúng Tôi</span>
                        </a>
                    </li>
                </ul>

                <div class="navbar-nav ms-auto">
                    <a class="nav-link" href="@Url.Action("Products", "Public")">
                        <i class="fas fa-gem"></i>
                        <span>Bộ Sưu Tập</span>
                    </a>
                    <a class="nav-link" href="tel:0909123456">
                        <i class="fas fa-phone"></i>
                        <span>0909.123.456</span>
                    </a>
                    <!-- TODO: Uncomment when cart is ready
                    <a class="nav-link" href="@Url.Action("Cart", "Public")">
                        <i class="fas fa-shopping-cart"></i>
                        <span>Giỏ Hàng</span>
                    </a>
                    -->
                </div>
            </div>
        </div>
    </nav>

    <!-- Main Content -->
    <main>
        @RenderBody()
    </main>

    <!-- Luxury Footer -->
    <footer class="luxury-footer">
        <div class="container">
            <div class="row">
                <!-- Brand Section -->
                <div class="col-lg-4 col-md-6 mb-4">
                    <div class="footer-brand">
                        <i class="fas fa-gem"></i>
                        <span>Jewelry Store</span>
                    </div>
                    <p class="footer-description">
                        Thương hiệu trang sức cao cấp hàng đầu Việt Nam. Chúng tôi mang đến những tác phẩm nghệ thuật tinh xảo, kết hợp hoàn hảo giữa truyền thống và hiện đại.
                    </p>
                    <div class="footer-social">
                        <a href="#" class="social-icon" aria-label="Facebook">
                            <i class="fab fa-facebook-f"></i>
                        </a>
                        <a href="#" class="social-icon" aria-label="Instagram">
                            <i class="fab fa-instagram"></i>
                        </a>
                        <a href="#" class="social-icon" aria-label="Youtube">
                            <i class="fab fa-youtube"></i>
                        </a>
                        <a href="#" class="social-icon" aria-label="TikTok">
                            <i class="fab fa-tiktok"></i>
                        </a>
                    </div>
                </div>

                <!-- Quick Links -->
                <div class="col-lg-2 col-md-6 mb-4">
                    <h5 class="footer-section-title">Liên Kết</h5>
                    <ul class="footer-links">
                        <li>
                            <a href="@Url.Action("Index", "Home")" class="footer-link">
                                <i class="fas fa-chevron-right"></i>
                                <span>Trang Chủ</span>
                            </a>
                        </li>
                        <li>
                            <a href="@Url.Action("Products", "Public")" class="footer-link">
                                <i class="fas fa-chevron-right"></i>
                                <span>Sản Phẩm</span>
                            </a>
                        </li>
                        <li>
                            <a href="@Url.Action("About", "Public")" class="footer-link">
                                <i class="fas fa-chevron-right"></i>
                                <span>Về Chúng Tôi</span>
                            </a>
                        </li>
                        <li>
                            <a href="@Url.Action("Contact", "Public")" class="footer-link">
                                <i class="fas fa-chevron-right"></i>
                                <span>Liên Hệ</span>
                            </a>
                        </li>
                    </ul>
                </div>

                <!-- Categories -->
                <div class="col-lg-3 col-md-6 mb-4">
                    <h5 class="footer-section-title">Danh Mục</h5>
                    <ul class="footer-links">
                        <li>
                            <a href="#" class="footer-link">
                                <i class="fas fa-ring"></i>
                                <span>Nhẫn Cao Cấp</span>
                            </a>
                        </li>
                        <li>
                            <a href="#" class="footer-link">
                                <i class="fas fa-star"></i>
                                <span>Dây Chuyền</span>
                            </a>
                        </li>
                        <li>
                            <a href="#" class="footer-link">
                                <i class="fas fa-circle"></i>
                                <span>Vòng Tay</span>
                            </a>
                        </li>
                        <li>
                            <a href="#" class="footer-link">
                                <i class="fas fa-gem"></i>
                                <span>Bông Tai</span>
                            </a>
                        </li>
                    </ul>
                </div>

                <!-- Contact Info -->
                <div class="col-lg-3 col-md-6 mb-4">
                    <h5 class="footer-section-title">Liên Hệ</h5>

                    <div class="footer-contact-item">
                        <div class="footer-contact-icon">
                            <i class="fas fa-phone"></i>
                        </div>
                        <div class="footer-contact-text">
                            <span class="footer-contact-label">Hotline</span>
                            <span class="footer-contact-value">0909.123.456</span>
                        </div>
                    </div>

                    <div class="footer-contact-item">
                        <div class="footer-contact-icon">
                            <i class="fas fa-envelope"></i>
                        </div>
                        <div class="footer-contact-text">
                            <span class="footer-contact-label">Email</span>
                            <span class="footer-contact-value">contact@jewelrystore.com</span>
                        </div>
                    </div>

                    <div class="footer-contact-item">
                        <div class="footer-contact-icon">
                            <i class="fas fa-map-marker-alt"></i>
                        </div>
                        <div class="footer-contact-text">
                            <span class="footer-contact-label">Địa chỉ</span>
                            <span class="footer-contact-value">TP. Hồ Chí Minh</span>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Footer Bottom -->
            <div class="footer-bottom">
                <p class="footer-copyright">
                    &copy; 2024 Jewelry Store. All rights reserved. Made with <i class="fas fa-heart footer-heart"></i> in Vietnam
                </p>
            </div>
        </div>
    </footer>

    <!-- Scroll to Top Button -->
    <button class="scroll-to-top" id="scrollToTop" onclick="scrollToTop()">
        <i class="fas fa-arrow-up"></i>
    </button>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>

    <script>
        // Scroll to Top functionality
        window.addEventListener('scroll', function() {
            const scrollBtn = document.getElementById('scrollToTop');
            if (window.pageYOffset > 300) {
                scrollBtn.classList.add('show');
            } else {
                scrollBtn.classList.remove('show');
            }
        });

        function scrollToTop() {
            window.scrollTo({
                top: 0,
                behavior: 'smooth'
            });
        }

        // Add active class to current page
        document.addEventListener('DOMContentLoaded', function() {
            const currentPath = window.location.pathname;
            const navLinks = document.querySelectorAll('.nav-link');

            navLinks.forEach(link => {
                link.classList.remove('active');
                if (link.getAttribute('href') === currentPath) {
                    link.classList.add('active');
                }
            });
        });
    </script>

    <!-- Render Section Scripts -->
    @RenderSection("Scripts", required: false)
</body>
</html>